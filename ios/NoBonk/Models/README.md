The optional Fast graph is intentionally not committed. From `ios/`, run:

`python3 scripts/setup_fast_model.py /path/to/existing/yolo26n_416.onnx`

Only SHA-256 `9931a595afd6c780bdbe5ef12e99d34526b6f30dbc7641cf920572798e4ed96d` is accepted. The script performs no download. Runtime verifies the bundled bytes again. Missing/incorrect assets leave Apple Vision People mode available. See `ios/FAST-OBJECTS.md` for runtime provenance and validation limits.
